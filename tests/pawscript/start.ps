testint => (): int {
  mut i = 5;
  // Do I allow for no mutability modifier,
  // and set const for default?
  // I won't allow, but maybe in the future
  beta_print i;

  i = 3;
  beta_print i;

  tmp => (j: int): int {
    // i needs to be const if we want to use it here!
    return i, j;
  }

  {
    // i not good
    const i = 88;
    beta_print i;
  }

  const b = true;
  if(b) {
    beta_print b;
  }

  return 0;
}

const mark: int = 0;
testint();
