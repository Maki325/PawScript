main => (): int {
  // Constants
  // Their value can't be changed during runtime
  const a = 78;

  // Mutables
  // Variables with mutable values of the same type
  mut b = 54;
  b = 777;

  return 0;
}
